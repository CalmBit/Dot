<%= form_for(@u2_f_registration) do |f| %>
  <% if @u2_f_registration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@u2_f_registration.errors.count, "error") %> prohibited this u2_f_registration from being saved:</h2>

      <ul>
      <% @u2_f_registration.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
